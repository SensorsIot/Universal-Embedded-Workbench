# Listen on all interfaces (0.0.0.0) for test network access
listener 1883 0.0.0.0

# Allow anonymous connections (test environment)
allow_anonymous true

# Also accept username/password auth
password_file /etc/mosquitto/passwd
